1) Что от нас хотят?
-  скрипт (файл. `#!/bin/bash`)
-  перезапускает сетевой интрефейс (? мы знаем что это такое ?) +
-  команды даны
-  запуск скрипта с позиционным аргументом
2) Как это реализовать (human)?
- проверки (?)
    - права root - потому что для операций с сетевыми устройствами нужен root
    - передан минимум 2 аргумента: имя интерфейса и действие (up/down)
    - существует ли такой интерфейс
        - **существует** - в зависимости от action выполнить команду по переводу в соответствующий статус (up/down)
        - **несуществует** - вывести сообщение о некорректном имени интерфейса
3) Пишем код
