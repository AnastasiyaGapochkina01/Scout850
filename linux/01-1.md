1) Написать скрипт для автоматической проверки и вывода свободного места на диске на сервере. Скрипт должен информировать, если свободного места осталось меньше заданного порога.
2) Сделать инсталлятор, проверяющий, запущен ли он от имени root. Если нет — вывести ошибку и завершить работу. Если да — автоматически установить необходимые пакеты через apt (git, curl, wget, jq)
3) Создать скрипт, который принимает аргументы командной строки и выводит их с пояснениями (имя скрипта, первый аргумент, второй, последний, список всех аргументов).
4) Написать скрипт поиска пользователя в файле /etc/passwd и выводит сообщение о том, найден ли пользователь.
